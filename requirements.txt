# Runtime dependencies for patient_maker.py.
# (JSON parsing uses the standard library — pandas is intentionally not needed.)
requests>=2.31.0
python-dotenv>=1.0.0

# For development/testing, install the dev extras instead:
#   pip install -e ".[dev]"
